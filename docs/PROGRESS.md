# Журнал прогресса стабилизации

> Живой журнал работ по [ROADMAP.md](./ROADMAP.md). Обновляется в конце каждого блока работ.
> Цель — в любой момент (даже после потери контекста) понять: **что сделано** и **что дальше**.
> Легенда статусов: ✅ сделано · 🔄 в работе · ⬜ не начато.

## Текущая позиция

**Сейчас:** 🔄 **Phase 5 почти завершена.** Phase 1, 2, 4 — бэкенд. По фронту сделаны этапы **D (редизайн)**, **A (гигиена)**, **B (модуляризация ESM)** + **оптимизация рендера логов** + добавлены **фронтенд E2E-тесты (Playwright, 14 шт., зелёные)**. Приложение переименовано в **DataStudio**. **Осталось по Phase 5: только этап C** (словарь UI-строк под i18n, чистка оставшихся inline-стилей в `updateSearchHistory`) + продолжать чистку лишнего по ходу. Дальше — backlog расширения функционала (ROADMAP §15: GraphQL и др.).

> **Следующей сессии:** читать этот файл целиком (раздел «Phase 5» ниже) + память. Состояние стабильно и проверяемо: см. «Как проверить» ниже. Коммиты делает владелец сам — изменения в рабочем дереве.

> **Важно про фронт (со слов владельца):** текущий дизайн НЕ нравится — «скупой», дешёвый. Нужен серьёзный редизайн: по-настоящему современный вид, удобные ввод/вывод, заметные функциональные ключевые кнопки, наглядные переключатели функций, неброская но интересная палитра. Детали — в ROADMAP §6 «Этап D». Phase 5 дробить на под-шаги и согласовывать визуал с владельцем.

> **На будущее (конец плана):** функционал будет расти (GraphQL — приоритетный кандидат, плюс YAML/CSV, diff, схемы и др.). Зафиксировано в ROADMAP §15. Не текущая работа.

### Как проверить текущее состояние (для владельца / следующей сессии)
1. **Запустить приложение** (Docker Compose, из корня проекта):
   `docker compose up --build -d` → открыть http://localhost:8080 (Ctrl+Shift+R для сброса кэша).
2. **Бэкенд-тесты (Java):** `docker compose run --rm test` — **41 тест, зелёные**.
3. **Фронтенд E2E (Playwright):** приложение должно быть поднято, затем из корня:
   `npm run test:e2e` (headless) или `npm run test:e2e:headed` (видимый браузер) — **14 тестов, зелёные**.
   Установка (если ещё нет): `npm install` + `npx playwright install chromium`. Подробно — [TESTING.md](./TESTING.md).

Maven также установлен локально (`mvn test`/`mvn spring-boot:run`), но поддерживаемый способ — Compose.
**Состояние на конец сессии 2026-05-30:** сборка проходит, 41 бэкенд-тест + 14 E2E зелёные, приложение поднимается и отдаёт новую разметку (DataStudio). Изменения — в рабочем дереве (без коммита).

---

## Статус по фазам

### Phase 1 — Низкорисковая стабилизация ✅ (2026-05-29)
- ✅ Удалён мёртвый код: `service/LogExtractor.java`, `service/BracketExtractor.java`, метод `XmlUtils.minify`.
  - `HashUtil` **сознательно оставлен** — будет переиспользован в Phase 2 для устранения дублей SHA-256.
- ✅ CORS: `allowedOrigins("*")` → список из конфига `app.cors.allowed-origins` (по умолчанию localhost). Файлы: `config/WebConfig.java`, `application.yml`.
- ✅ XSS: добавлен `escapeHtml(...)` в `static/app.js`; `highlightText` теперь всегда экранирует вход перед вставкой в `innerHTML` (включая ветки «без подсветки» и `catch`).
- ✅ Добавлен `.gitignore` (target/, логи, IDE).
- **Не делалось** (перенесено): удаление `console.log` из прод-путей — мелочь, заберём в Phase 5 вместе с фронтом.

### Phase 3 — Тестовый фундамент 🔄 (стартовал вместе с Phase 1, расширен в Phase 2)
Характеризационная сеть (34 теста, все зелёные):
- ✅ `JsonServiceTest` (6): pretty/minify/validate, wrap, **wrap→unwrap round-trip без потери данных**.
- ✅ `XmlServiceTest` (3): pretty/minify/validate.
- ✅ `LogServiceTest` (3): извлечение по паттерну `request`, **дедупликация**, пустой вход.
- ✅ `JsonUtilsTest` (4): стабильность канонического хеша к порядку ключей, компактный `value`, счётчики.
- ✅ `UniversalJsonExtractorTest` (4): `extractFromArgs`, `findAllJsonInText`.
- ✅ `XmlUtilsTest` (3): well-formed parse, **защита от XXE (DOCTYPE отклоняется)**, python-style one-line.
- ✅ `JsonBalancerTest` (11): крайние случаи балансировки — скобки/кавычки внутри строк, экранирование, хвост, несбалансированный вход.
- ⬜ Осталось на будущее: контроллерные тесты `@WebMvcTest`.

### Phase 2 — Очистка и дедупликация ✅ (2026-05-29)
- ✅ Создан общий `util/JsonBalancer.extractCompleteJson(...)`; приватные копии удалены из `LogService` и `UniversalJsonExtractor`. Контракт зафиксирован 11 тестами.
- ✅ SHA-256 сведён на единый `HashUtil` (добавлена перегрузка для `byte[]`): переведены `LogService.sha256` (удалён), `JsonUtils.canonicalHash`, `XmlUtils.structuralHashStrict/Normalized`. Хеши не изменились (проверено тестами + смоук).
- ✅ Фронт: два почти идентичных обработчика `btnWrap`/`btnUnwrap` (~120 строк) сведены в `handleJsonValueAction(action)` + конфиг `JSON_VALUE_ACTIONS`. Поведение 1:1.
- Проверено в живом контейнере: wrap/unwrap round-trip, normalize, xml pretty — корректно.

### Phase 4 — Структурные улучшения ✅ (2026-05-29)
- ✅ **A) Логирование (slf4j):** добавлен логгер в `LogService`; 3 молчаливых `catch (Exception ignore)` заменены на `log.debug(...)` (намеренные пропуски, поведение отсева НЕ изменено). Уровень `com.example.formatter` задаётся в `application.yml` (по умолчанию INFO).
- ✅ **B) Глобальная обработка ошибок:** `controller/ApiExceptionHandler` (@RestControllerAdvice) — непойманные исключения → читаемый JSON с `errors` + лог стектрейса (вместо голого 500). Покрыт `ApiExceptionHandlerTest`. Штатные ошибки сервисов не затронуты.
- ✅ **C) `LogService.normalize` разбит** на приватные шаги: `resolveEnabledPatterns` / `processEntry` / `resolveTextForPatterns` / `addExtractedMessage` + аккумулятор `Acc`. Логика 1:1. ПРЕДВАРИТЕЛЬНО заложены защитные тесты `LogServiceFilteringTest` (6) на нюансы отсева — они же задокументировали квирк: фильтр SERVICE_FIELDS работает только в ветке afterColon, а универсальный fallback извлекает служебный JSON с ключом `found_json_1`.
- ⏭️ **D) (пропущено сознательно)** перенос `UniversalJsonExtractor` `util`→`service` — низкая польза / лишний churn. Можно сделать позже при желании.

### Phase 5 — Подготовка фронтенда + редизайн 🔄 (2026-05-30)
Порядок изменён по запросу владельца: сначала **D (визуал)**, чтобы согласовывать вид; гигиена/модуляризация — следом.

**Этап D — визуальный редизайн ✅ (в основном сделан, принят владельцем):**
- Новая дизайн-система на CSS-токенах: тёмная тема по умолчанию + светлая (`data-theme`), палитра «сланец + пыльный индиго», переключатель темы в сайдбаре. Файл `styles.css` переписан целиком.
- Левый **сайдбар-навигация** вместо верхнего меню (масштабируется под будущие форматы). Приложение переименовано **Formatter → DataStudio** (видимый бренд; имя docker-образа пока не менялось).
- Главная упрощена (карточки Logs/JSON/XML). JSON/XML: короткие описания; **поля ввода и вывода равны и выровнены** (макет `io-layout`: поля в ряд, действия под ними); единый файловый пикер с состоянием.
- **Логи разделены на 2 страницы:** ввод (`#logs`) → результат (`#logsResult`) с кнопкой «Назад к вводу» и кнопкой «Последний результат»; роутинг через `navigate()`/`onRoute()` (+ `logsResult` подсвечивает «Логи»).
- **Подсветка синтаксиса JSON** (`syntaxHighlightJson`, токены `tok-*`) в выводе логов и JSON-форматтере; яркая красная подсветка поиска.
- UI логов почищен: паттерны извлечения убраны (шлём полный набор константой `ALL_EXTRACTION_PATTERNS`, поведение 1:1), traceID вместо хеша в шапке карточки, окно сравнения расширено (без горизонтального разъезда).
- **Багфиксы:** двойной интервал в выводе логов; **фильтр «Только уникальные/Показывать дубликаты»** реально не работал (searchLogs затирал фильтр) — перенесён в `searchLogs`, стал реактивным; тумблер «Подсветка» теперь даёт видимый эффект.
- **Новая функция:** в JSON (режим Value Wrapper) кнопка **«🔢 Числа → value»** — собирает JSON-массив `{"value":"<n>"}` из набора чисел (любой разделитель, value строкой). Полностью на клиенте.
- В план (ROADMAP §15) добавлена идея владельца: **анализ сообщений по уровню/ошибкам** (INFO/DEBUG/ERROR/internal), а не только извлечение JSON. Сейчас фильтры по типу (request/response/internal/external) работают по `key`; INFO/DEBUG/ERROR неактивны (бэкенд не возвращает `level`/`raw`).

**Этап A — гигиена ✅ (2026-05-30):**
- ✅ Единый HTTP-слой: wrap/unwrap переведены с сырого `fetch` на общий `postJson`.
- ✅ Убраны `alert()` в JSON-обёртке → `showNotification`; убран прод-шум `console.log`.
- ✅ **DOM вместо innerHTML в карточках логов:** `buildItemCard` собирает узлы через помощник `el()` (`core/dom.js`), обработчики — `addEventListener` вместо inline-`onclick`. Ушёл класс XSS через разметку и зависимость от глобальных функций: в `window` остались только `navigate` и `closeCompareModal` (для inline в статической разметке). `html`-вставка осталась только для уже экранированных фрагментов подсветки.
- ✅ **`prompt()` → неблокирующий `<dialog>`** (jumpToMessage / Ctrl+G).

**Этап B — модуляризация ✅ (2026-05-30, СТРОГИЙ ESM):**
Монолит `app.js` (~1357 строк) разбит на ESM-модули под `static/js/`:
- `core/` — `api.js` (postJson/postMultipart), `dom.js` (setText/downloadText/formatBytes/debounce/escapeHtml/showNotification), `highlight.js` (highlightText/syntaxHighlightJson/extractTraceId/formatJsonWithLineNumbers), `router.js` (navigate/onRoute), `theme.js`.
- `tools/` — `logs.js` (всё по логам; **состояние логов приватно внутри модуля** — отдельный `state.js` не понадобился, т.к. состояние не пересекает границы модуля), `json.js`, `xml.js`.
- `main.js` — точка входа (`<script type="module">`), выносит в `window` функции для inline-onclick (navigate + действия карточек логов).
- Поведение 1:1 (ничего не оптимизировал намеренно — чистый структурный diff). Старый `app.js` оставлен на диске как фолбэк (НЕ подключён) — удалить после ревью владельцем. Браузерных тестов нет → проверка: синтаксис всех модулей (node), отдача всех файлов (200), смоук API. **Функциональный прогон в браузере — за владельцем.**

**Чистка проекта (начата 2026-05-30, по запросу владельца — «потихоньку»):**
- ✅ Удалён мёртвый `static/app.js` (1357 строк, заменён ESM-модулями).
- ✅ Вырезан мёртвый CSS: чипы-паттерны `.pat*`, осколки старого макета логов (`.logs-layout`/`.logs-input` в медиа-запросе). `.two-cols/.left/.right` ушли ещё при переходе на `io-layout`.

**Этап C — консистентность ✅ (2026-05-30):**
- ✅ **Словарь UI-строк** `core/strings.js` (единый источник, одна локаль ru) — по выбору владельца **полный вынос ВСЕХ русских строк**: уведомления, заголовки, подписи кнопок, плейсхолдеры, тексты карточек. Английские токены (Pretty/Minify/JSON, имена файлов) намеренно не выносились.
- ✅ **Статическая разметка через `data-i18n`/`data-i18n-ph`/`data-i18n-title`** + `core/i18n.js` `applyI18n()` (раскладывает словарь по DOM на старте). Подписи с input внутри `<label>` обёрнуты в `<span>`, чтобы заполнять только текст. Брендовые/английские значения оставлены литералами.
- ✅ Смесь RU/EN убрана: карточка главной **«Logs» → «Логи»** (в один ряд с навигацией).
- ✅ **Inline-стили убраны** из JS: `updateSearchHistory` (label → класс `.search-history-label`), drag&drop подсветка (→ класс `.dragover`), pulse-анимация перехода к сообщению (→ класс `.item-card.pulse` + реальные `@keyframes pulse`; раньше inline ссылался на несуществующие keyframes и молча не работал). Data-driven `bar.style.height` в мини-графике оставлен (легитимный inline).
- Не выносил: `buildSeparator`/«Без описания» — это формат экспортного файла, зеркалящий вывод бэкенда, и backend-сентинел (контракт, не UI-хром).
- Проверка: синтаксис всех модулей (node), образ пересобран, **17 E2E + 38 ui-full зелёные** (правки формулировок сторожатся тестами; обновлён тест карточки «Логи»). Без коммитов.

**Осталось по Phase 5:**
- 🔁 Чистка лишнего — продолжать по ходу (мёртвый CSS/код, дубли).
- ✅ **Производительность рендера логов (2026-05-30):** `renderLogItems` переписан на **пакетный рендер** (по 150 карточек + кнопка «Показать ещё») через `DocumentFragment` (вставка пачки одним рефлоу) + **кэш синтаксической подсветки** на элементе (`item._synHi` — regex по `pretty` считается один раз). Снижены и память (не тысячи DOM-узлов сразу), и стоимость каждого перерендера (например, на ввод в поиске). Поиск/фильтр/экспорт/счётчики по-прежнему по полному набору. `jumpToMessage` дорендеривает остаток при необходимости. Покрыто E2E (`logs.spec.js` — 150 + «Показать ещё»). **11 E2E зелёные.**

> Менять обработку логов только под защитой тестов — владелец к этому особо чувствителен (бэкенд логов не трогали).

### Phase 6 — Опциональная декомпозиция ⬜
По умолчанию — не делать. Только при кратном росте функциональности.

---

## История изменений
- **2026-05-29** — Phase 1 выполнена + заложена тестовая сеть (Phase 3). 23 теста зелёные. Коммитов не делалось по просьбе (изменения в рабочем дереве).
- **2026-05-29** — Phase 2 выполнена: унификация `extractCompleteJson` (JsonBalancer) и SHA-256 (HashUtil), дедуп wrap/unwrap на фронте. 34 теста зелёные, смоук в контейнере пройден. Без коммитов.
- **2026-05-29** — Phase 4-A/B: slf4j-логирование вместо молчаливых catch + глобальный @RestControllerAdvice. 35 тестов зелёные, смоук (400 на битом теле, happy-path ок). Без коммитов.
- **2026-05-29** — Phase 4-C: `LogService.normalize` разбит на приватные шаги под защитой 6 новых тестов отсева (поведение 1:1). Пункт D пропущен. 41 тест зелёный, смоук logs/normalize ок. Без коммитов.
- **2026-05-30** — **Фикс 4 дефектов (по баг-репорту владельца):** (1) убрана спам-всплывашка автосохранения (тихий автосейв); (2) тумблер «Подсветка» сделан понятным мастер-переключателем (ВЫКЛ → обычный текст, ВКЛ → синтаксис/совпадения); (3) **backend `unwrap`**: разворачивает обёртки `{"value":...}` и как ЭЛЕМЕНТЫ массива (`isValueWrapper` + ветка массива) — было `[{"value":"22341"}]` не разворачивалось; (4) сообщения о пустом вводе (уже были) — уточнены формулировки. Тесты: **42 backend** (+1 на unwrap), **17 E2E** (+подсветка, +unwrap, +пустой ввод) — все зелёные. Без коммитов.
- **2026-05-30** — Phase 5 этап **A завершён**: карточки логов на DOM-узлах (`el()`) вместо innerHTML/inline-onclick (минус XSS-вектор, минус глобалы), `prompt()`→`<dialog>`. **Чистка:** удалён мёртвый `app.js`, вырезан мёртвый CSS. E2E расширены до **14** (взаимодействия карточек, сравнение, Ctrl+G, пакетный рендер) — все зелёные. Без коммитов.
- **2026-05-30** — **Производительность рендера логов**: пакетный рендер (150 + «Показать ещё») + DocumentFragment + кэш подсветки. Снижает CPU/память на больших логах. Покрыто E2E (11 тестов зелёные). Без коммитов.
- **2026-05-30** — **Добавлены фронтенд E2E-тесты (Playwright)**: 10 тестов в `e2e/` (navigation/json/xml/logs), все зелёные против запущенного контейнера. Закрыт пробел «нет браузерных автотестов». Установка/запуск — `docs/TESTING.md`. Теперь фронт-изменения (в т.ч. предстоящая оптимизация рендера) проверяемы автоматически. Dev-only (`package.json`/`playwright.config.js`), на поставку приложения не влияет.
- **2026-05-30** — Phase 5 этап **B (модуляризация)**: `app.js` разбит на ESM-модули `core/` + `tools/` + `main.js` (строгий ESM, состояние логов инкапсулировано). Этап A частично (HTTP-слой, alert, console.log). Поведение 1:1; синтаксис/отдача проверены, браузерный прогон — за владельцем. Старый `app.js` оставлен фолбэком. Без коммитов (владелец коммитит сам).
- **2026-05-30** — **Phase 5 этап C (консистентность)**: создан единый словарь UI-строк `core/strings.js` + `core/i18n.js` (`applyI18n` через `data-i18n`); по выбору владельца — **полный вынос всех русских строк** из `index.html` и всех JS-модулей. Убрана смесь RU/EN (карточка «Logs»→«Логи»). Убраны hardcoded inline-стили (`.search-history-label`, `.dragover`, `.item-card.pulse` + реальные `@keyframes pulse`). Образ пересобран; **17 E2E + 38 ui-full зелёные**. Без коммитов.
- **2026-05-30** — **Полный UI-прогон (исчерпывающие E2E)**: добавлен отдельный проект `ui-full` (`e2e-full/`, 4 файла, **38 тестов**) — прокликивает каждую кнопку/действие во всех разделах на тестовых JSON/XML/логах со всеми условиями. Запуск `npm run test:ui-full` — **всегда видимый браузер + замедление `slowMo:550мс`**, 1 поток; гонять по готовности крупного блока, не каждый раз. Быстрый дымовой набор разведён в проект `chromium` (`e2e/`, 17 тестов). При первом прогоне набор сразу вскрыл 5 ложных допущений в моих тестах (validate без success-уведомления; input-page чекбоксы скрыты на результатах; звезда по hover) — исправлено. Оба набора зелёные. Детали и грабли — `docs/TESTING.md`. Без коммитов.
- **2026-05-30** — Phase 5 этап D (визуальный редизайн) в основном выполнен и принят владельцем: новая дизайн-система (тёмная/светлая темы, сайдбар, палитра сланец+индиго), переименование в **DataStudio**, двухстраничные логи, подсветка синтаксиса, выравнивание полей JSON/XML, фикс фильтра уникальных/дубликатов, новая функция «Числа → value». Бэкенд не трогали. **Владелец сам сделал коммит** (зафиксировал состояние). Дальше — этапы A (гигиена), B (модуляризация app.js), C (консистентность).
